name: agent_forge
description: "A minimalist AI Agent generator PWA."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  provider: ^6.1.1
  archive: ^3.6.1
  url_launcher: ^6.2.4
  intl: ^0.19.0
  supabase_flutter: ^2.8.3
  flutter_markdown: ^0.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
